# ? 0
# ? 1
# > 46368
# > 28657

PROCEDURE proc(T tab) IS
BEGIN
    WRITE tab[2];
END


PROGRAM IS
    x,tab[0:20]
BEGIN

    FOR i FROM 0 TO 10 DO
        tab[i] := 10 - i;
        WRITE tab[i];
    ENDFOR

    proc(tab);

END
