# ? 0
# ? 1
# > 46368
# > 28657

PROCEDURE proc(T tab, start, end, multi) IS
BEGIN
    FOR i FROM start TO end DO
        tab[i] := tab[i] * multi;
        WRITE tab[i];
    ENDFOR
END


PROGRAM IS
    tab[0:20], start, end, multi
BEGIN
    start := 0;
    end := 20;
    multi := 10;

    FOR i FROM 0 TO 20 DO
        tab[i] := i;
    ENDFOR

    proc(tab, start, end, multi);

END
